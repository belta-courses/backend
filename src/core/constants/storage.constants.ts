const ONE_MB = 1024 * 1024;

const ONE_MINUTE = 60 * 1000;
const ONE_HOUR = 60 * ONE_MINUTE;

export const VIDEO_PART_SIZE = 5 * ONE_MB;
export const MAX_VIDEO_SIZE = 100 * ONE_MB;
export const MAX_PART_RETRIES = 3;
export const INACTIVE_TIME_TO_ABBORFT_UPLOAD = 2 * ONE_HOUR;
